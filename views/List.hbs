<html>
<h1>SEARCH</h1>
<form id="update-playlist" action="/playlists/{{playlist.playlist_id}}" method="post">
    Produce: <select name="name" id="ID"> 
      {{#each users_dropdown}}
      <option value="{{ID}}">{{name}}</option>
                   {{/each}}
    </select><br>
</form>
<button onclick="updatePlaylist({{playlist.playlist_id}})">SEARCH</button>
<script defer>selectPlaylist({{playlist.uid}});</script>
</html>
